export const METHOD_GENERATE_TOKEN_PAIR = 'generateTokenPair';

export const METHOD_AUTHENTICATE = 'authenticate';

export const METHOD_CHECK_AVAILABLE_TOKENS = 'checkAvailableTokens';

export const METHOD_MAKE_LOGOUT = 'makeLogout';
